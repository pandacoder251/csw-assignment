# Q1 Solution

# Prompt user
x, y, z = map(int, input("Enter three numbers separated by spaces: ").split())

print(f"Before swapping: x={x}, y={y}, z={z}")

# Swap y and z in one line
y, z = z, y

print(f"After swapping: x={x}, y={y}, z={z}")
